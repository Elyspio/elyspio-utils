/**
 * Kept for IDE compatibility while Vite+ uses the `fmt` block in `vite.config.ts`.
 *
 * @type {import("prettier").Config}
 */
const config = {
	printWidth: 180,
	singleQuote: false,
	tabWidth: 4,
	useTabs: true,
	trailingComma: "es5",
};

export default config;
